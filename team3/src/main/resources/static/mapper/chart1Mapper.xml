<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vet.clinic.dao.Chart1DAO">
	<select id="petdetailAjax" resultType="Hashmap" parameterType="String"> 
		SELECT pet_name, pet_birth, pet_gender, pet_weight, type_name, owner_name, pet_no
		FROM receptionview
		WHERE receive_no = #{receiveno};
	</select>
	
	<select id="prescAjax" resultType="Hashmap">
		SELECT medical_category, medical_name, FORMAT(medical_price , 0) AS medical_price
		FROM medicalList
		ORDER BY medical_category
	</select>
	<select id="prescAjaxvac" resultType="Hashmap">
		SELECT vac_name, FORMAT(vac_price , 0) AS vac_price
		FROM vaccine
	</select>
	
</mapper>